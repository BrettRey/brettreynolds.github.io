
# Review-board archives: raw AI-generated critiques written in the voice of real,
# named people. Preserved locally per the agent-output-management rule, but not
# published, because a public file named opus-lakoff.md reads as a real review.
reviews/
